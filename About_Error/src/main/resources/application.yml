spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

  devtools:
    livereload:
      enabled: true
    remote:
      restart:
        enabled: true

  jpa:
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/about_error?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: 1234
